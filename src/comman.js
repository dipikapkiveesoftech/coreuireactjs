{/*export class CommonService {
    rootUrl = 'http://localhost:3000/api/v1/';
    httpOptionSecure = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' })
    };
  
    constructor() {
    }
  
    httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json', 'Access-Control-Allow-Origin':'http://localhost:3000/api/v1/' })
    };
  
    http_request = new XMLHttpRequest();
  
    apiPost(path, param) {
      return fetch(this.rootUrl + path, param, this.httpOptionSecure);
    }
  
    apiGet(path, param) {
      return fetch(this.rootUrl + path, this.httpOptionSecure);
    }
  
    apiUpload(path, param) {
      return fetch(this.rootUrl + path, param);
    }
  
    apiPatch(path, param) {
      return fetch(this.rootUrl + path, param);
    }
  
    apiDelete(path, param) {
      return fetch(this.rootUrl + path, param);
    }
}*/}